Trip screen + range-aware fuel planner (cumulative update against your current main)

Upload over the existing repo, same folder layout:
  app/page.tsx         (modified)
  app/globals.css      (modified)
  app/map.tsx          (modified)
  app/trip-map.tsx     (NEW)
  app/trip-planner.tsx (NEW)
  lib/fuel.ts          (NEW)
  lib/tripOrder.ts     (NEW)

This includes the earlier fuel-planner update, so it is safe whether or not you
already uploaded that one.

GitHub web upload: repo > Add file > Upload files, drag the "app" and "lib"
folders from this zip (not the zip itself), commit. Vercel redeploys.
No new packages, no new environment variables, no database migration.

trip-screen-and-fuel-planner.patch is the same change for: git apply <file>
